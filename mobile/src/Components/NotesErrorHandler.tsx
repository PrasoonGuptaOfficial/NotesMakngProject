import React from 'react';
import {Text, View} from 'react-native';

const NotesErrorHandler = (): JSX.Element => {
  return (
    <View>
      <Text>ABC</Text>
    </View>
  );
};

export default NotesErrorHandler;
